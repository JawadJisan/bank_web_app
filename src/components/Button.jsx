import React from 'react';

const Button = () => {
    return (
       <button type='button' className={`py-4 rounded-lg px-6 font-poppins font-medium text-[19px] text-primary outline-none mt-10 bg-blue-gradient`}>
        Get Started

       </button>
    );
};

export default Button;